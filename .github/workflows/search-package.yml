name: search-package

on:
  push:
    paths:
      - "VERSION"
      - "packages/core/**"
      - "packages/artifacts/**"
      - "packages/navigation/**"
      - "packages/search/**"
      - "scripts/check_package_boundaries.py"
      - "scripts/release_version.py"
      - "package-boundaries.toml"
      - "pyproject.toml"
      - ".github/workflows/search-package.yml"
  pull_request:
    paths:
      - "VERSION"
      - "packages/core/**"
      - "packages/artifacts/**"
      - "packages/navigation/**"
      - "packages/search/**"
      - "scripts/check_package_boundaries.py"
      - "scripts/release_version.py"
      - "package-boundaries.toml"
      - "pyproject.toml"
      - ".github/workflows/search-package.yml"

jobs:
  validate-search:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install validation tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine pytest ruff mypy "numpy<2.0"
      - name: Check coordinated release version
        run: python scripts/release_version.py check
      - name: Build dependency and Search wheels
        run: |
          python -m build packages/core
          python -m twine check packages/core/dist/*
          python -m build packages/artifacts
          python -m twine check packages/artifacts/dist/*
          python -m build packages/navigation
          python -m twine check packages/navigation/dist/*
          python -m build packages/search
          python -m twine check packages/search/dist/*
      - name: Lint and type-check Search package
        run: |
          python -m ruff check packages/search/src packages/search/tests
          python -m ruff format --check packages/search/src packages/search/tests
          python -m mypy packages/search/src
      - name: Check package boundaries
        run: python scripts/check_package_boundaries.py
      - name: Install wheels in clean environment
        shell: pwsh
        run: |
          python -m venv build/search-ci-venv
          $python = "build/search-ci-venv/Scripts/python.exe"
          & $python -m pip install --upgrade pip
          & $python -m pip install pytest
          python scripts/release_version.py install --python $python core artifacts navigation search
          & $python -m pip check
          & $python -m pytest -q packages/search/tests
      - name: Verify installed import locations
        shell: pwsh
        run: |
          $python = "build/search-ci-venv/Scripts/python.exe"
          & $python -c "import inspect, zeromodel.core, zeromodel.artifacts, zeromodel.navigation, zeromodel.search; print(inspect.getfile(zeromodel.core)); print(inspect.getfile(zeromodel.artifacts)); print(inspect.getfile(zeromodel.navigation)); print(inspect.getfile(zeromodel.search))"

