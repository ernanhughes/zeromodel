# Keep NumPy on the 1.x stub format while mypy targets the repository's
# Python 3.10 floor. NumPy 2.2+ uses Python 3.12 PEP 695 `type` statements in
# installed stubs, which mypy correctly rejects under a 3.10 target.
numpy>=1.23,<2.0

-e ./packages/core
-e ./packages/analysis
-e ./packages/observation
-e ./packages/vision
-e ./packages/video
-e ./packages/sqlalchemy
-e ./packages/artifacts
-e ./packages/trust
-e ./packages/navigation

build
twine
pytest
pytest-cov
ruff
mypy
cryptography>=41
tomli>=2; python_version < "3.11"

# Required by examples/render_signs_demo.py (image/GIF asset generation).
matplotlib
pillow
